# Build Scripts

Public build scripts belong here when they build source artifacts from reproducible public inputs.

Do not place deployer secrets, Cloudflare IDs, mainnet funding paths, smoke collectors, or key rotation scripts here.
