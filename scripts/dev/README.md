# Dev Scripts

Public dev scripts should be local-only, synthetic, and safe by default.

They must not broadcast mainnet transactions, fund accounts, rotate keys, collect live mainnet evidence, or require private operator state.
