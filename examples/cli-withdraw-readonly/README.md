# CLI Withdraw Read-Only Example

The public CLI example is read-only by default.

```bash
nullark config show --network megaeth-mainnet
nullark artifacts resolve --network megaeth-mainnet
```

Mainnet CLI submission is not enabled in this repo. Build a withdrawal plan, verify the public inputs, and submit through an authorized wallet flow.
