# Proving Artifacts

Reserved for public proving-artifact identity files.

The current machine-readable runtime source of truth is `../current.json`. Browser proving binaries are served from `apps/web/public/proving/`.

The checked-in public proving presentation follows the v1.2 runtime identified by `../current.json`. Historical v1.1 bundles are preservation context only and are not the active trusted setup or browser-prover identity.
