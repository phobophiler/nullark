# Circuit Sources

Current mainnet provenance records still bind the deployed browser proving artifacts to `circuits/private_transfer.circom` and `circuits/withdraw.circom`.

Reserved for the productized circuit-source layout. Move source files here only when the public artifact record and build scripts are updated in the same change.
